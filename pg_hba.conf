# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
# Require cert verification for all connections over the network wire
hostssl all             all             10.0.0.0/8              cert